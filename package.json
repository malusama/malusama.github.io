{
  "name": "malu-blog-presentation",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/enhance.mjs",
    "test": "node --test scripts/verify.test.mjs",
    "check": "npm run build && npm test && npm audit"
  },
  "engines": {"node": ">=22.12.0"},
  "dependencies": {"fuse.js": "7.5.0"},
  "devDependencies": {"cheerio": "1.2.0"}
}
