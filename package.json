{
  "name": "malu-blog",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:drafts": "node scripts/build.mjs --drafts",
    "new": "node scripts/new-post.mjs",
    "preview": "python3 -m http.server 4178 --bind 127.0.0.1 --directory public",
    "test": "node --test scripts/*.test.mjs",
    "check": "npm run build && npm test && npm audit"
  },
  "engines": {"node": ">=22.12.0"},
  "dependencies": {"fuse.js": "7.5.0"},
  "devDependencies": {"cheerio": "1.2.0"}
}
