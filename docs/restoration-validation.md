# Hugo 源码恢复验证

验证日期：2026-09-07。对比基准：`1d902498f4f74f6a94ec74058a38899e3b061989`，其首页 HTML 与当天读取的线上页面一致。

- Node.js 24.20.0、Hugo 0.165.0 下 `npm run check` 通过：7 项测试，依赖审计 0 个漏洞。
- 原有 36 篇文章的正文哈希、代码块、图片数量和标题锚点保持一致。
- 149 个历史 HTML 路径保留；99 个非跳转页面的 body DOM 去除不影响显示的空白后与基准一致。
- `blog.css`、`blog.js`、`theme.js` 三个文件未作修改，SHA256 与基准相同。
- 从纯源码的独立临时目录测试 Markdown 草稿、发布、删除；首页、归档、分类、标签、搜索、RSS、站点地图及相邻文章导航均同步更新。
- Chromium 检查：首页、归档、重庆文章、流式计算文章、关于、404；视口 1440×900 与 390×900；深色和浅色模式。对比关键元素位置、大小、字体、颜色，无水平溢出。11 组页面/尺寸的读数完全一致；流式计算手机页的正文及后续区域高度读数相差约 0.18 CSS 像素，DOM 和样式仍一致。
- 浏览器交互检查：搜索“重庆”得到 1 篇正确文章，配色切换、图片弹窗打开/关闭正常；移动端目录默认折叠，桌面重新加载后展开。未点击评论前没有请求 utteranc.es。
- 浏览器截图接口超时，因此没有进行截图像素对比；上述视觉保持证据来自 DOM、资产哈希和浏览器实际布局/样式读数。
- Actions YAML 已在本地解析。GitHub Actions 尚未运行，Pages 设置尚未变更，也未部署线上。

当前保留旧文章 HTML 正文，并非恢复出作者原始 Markdown。保留现有 Node 展示增强阶段；直接执行 Hugo 不代表完整的最终预览，请使用 README 中的 npm 命令。
